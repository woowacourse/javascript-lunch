import { StoreType } from '../type/common';

const store: StoreType = {
  domain: null,
  restaurants: [],
  categorySelector: '전체',
  sortSelector: 'name',
  isFavorite: false,
};

export default store;
